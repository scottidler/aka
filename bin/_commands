# bin/_commands

_commands() {
  # 1) grab your alias names
  local -a aka_aliases
  aka_aliases=( ${(f)"$(aka __complete_aliases)"} )

  # 2) add them to the “command‐name” completion list
  compadd -a aka_aliases

  # 3) then fall back to the normal executables
  _command_names
}
